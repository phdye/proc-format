END-EXEC;
EXEC ORACLE OPTION (...)
EXEC SQL AT :db-name
EXEC SQL AT :db-name COMMIT;
EXEC SQL AT :db-name DECLARE csr CURSOR FOR
EXEC SQL AT :db-name EXECUTE
EXEC SQL BEGIN DECLARE SECTION;
EXEC SQL CLOSE csr;
EXEC SQL COMMIT RELEASE;
EXEC SQL CONNECT :connect AT :db-name;
EXEC SQL CONNECT :connect IDENTIFIED BY :hostvar USING :db-string;
EXEC SQL CONNECT :connect IDENTIFIED BY :hostvar;
EXEC SQL CONNECT :connect;
EXEC SQL END DECLARE SECTION;
EXEC SQL FETCH csr
EXEC SQL INCLUDE SQLCA;
EXEC SQL OPEN csr;
EXEC SQL ROLLBACK [...];
EXEC SQL WHENEVER NOT FOUND DO ...;
EXEC SQL WHENEVER NOT FOUND GOTO identifier;
EXEC SQL WHENEVER SQLERROR CONTINUE;
EXEC SQL WHENEVER SQLERROR DO ...;
EXEC SQL sql-query
